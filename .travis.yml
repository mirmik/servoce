language: python

os: linux
python:
  "3.7"
      
install: 
  - echo "INSTALL"
  - pwd
  - ls

  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  - sudo apt-get update
  - sudo apt install libocct-* -y
  - python3 -m pip install licant --user

  - python3 ./setup.py install --user

script:
  - echo "SCRIPT"
  - pwd
  - ls

